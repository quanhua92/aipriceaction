/// Application configuration
pub struct Config {
    // TODO: Add configuration fields
}

impl Config {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Config {
    fn default() -> Self {
        Self::new()
    }
}
