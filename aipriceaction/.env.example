# Database URL for sqlx CLI
DATABASE_URL=postgresql://aipriceaction:helloaipriceaction@localhost:5432/aipriceaction

# Environment Configuration
# Set to "production" to enable production security features
AIPRICEACTION__ENV=development

# Database Configuration for application
AIPRICEACTION__DATABASE__USER=aipriceaction
AIPRICEACTION__DATABASE__PASSWORD=helloaipriceaction
AIPRICEACTION__DATABASE__HOST=localhost
AIPRICEACTION__DATABASE__PORT=5432
AIPRICEACTION__DATABASE__DATABASE=aipriceaction

# VCI Workers (background sync from VCI API to PostgreSQL)
# Set to "false" or "0" to disable daily/hourly/minute/dividend workers
VCI_WORKERS=true

# HTTP Proxies for VCI API (comma-separated, increases rate limit throughput)
# Each proxy adds a client with 60 req/min rate limit
# Example: HTTP_PROXIES=http://proxy1:8080,http://proxy2:8080
# HTTP_PROXIES=
